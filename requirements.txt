--extra-index-url https://download.pytorch.org/whl/cu130

# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
librosa>=0.10.0
pretty_midi>=0.2.10
pyfluidsynth
sounddevice

# Machine Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# CYOLO dependency: log-frequency filterbank used by LogSpectrogram in custom_modules.py.
# madmom 0.16.1 uses removed numpy type aliases (np.int, np.float, …); a
# compatibility shim is applied at runtime in models/cyolo_model.py so that
# it works alongside numpy>=1.24.
git+https://github.com/CPJKU/madmom

# Computer Vision (for CYOLO)
opencv-python>=4.8.0
pillow>=10.0.0
music21

# DTW
fastdtw>=0.3.4
dtaidistance>=2.3.10

# Data processing
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Audio I/O
soundfile>=0.12.0
audioread>=3.0.0

# Progress bars
tqdm>=4.65.0

# Configuration
pyyaml>=6.0

# Jupyter (optional, for notebooks)
jupyter>=1.0.0
ipywidgets>=8.0.0
